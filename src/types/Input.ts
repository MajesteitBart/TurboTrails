export interface PlayerInputState {
  throttle: boolean;
  brake: boolean;
  tiltBack: boolean;
  tiltForward: boolean;
  jumpPressed: boolean;
  jumpHeld: boolean;
  restartPressed: boolean;
  pausePressed: boolean;
}
